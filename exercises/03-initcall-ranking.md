# Exercise — Rank initcall Cost

Use:

```bash
python3 scripts/parse-initcall-debug.py sample-data/initcall/initcall-debug-sample.log
```

Then answer:

- Which initcall is slowest?
- Is it boot-critical?
- Is it waiting for hardware?
- What evidence would you collect next?
